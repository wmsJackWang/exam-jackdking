import { PartialObserver } from './observable';
export declare class Subscriber<T> {
    closed: boolean;
    syncErrorThrowable: boolean;
    private destinationOrNext;
    constructor(destinationOrNext: PartialObserver<any> | ((value: T) => void));
    next(value: T): void;
    error(err: any): void;
    complete(): void;
}
