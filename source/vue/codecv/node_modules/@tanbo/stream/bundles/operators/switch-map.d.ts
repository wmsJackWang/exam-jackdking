import { Observable, Operator } from '../core/_api';
/**
 * 返回一个新的数据流，并以新数据流的订阅结果，发送出去
 * @param handle
 */
export declare function switchMap<T, U>(handle: (value: T) => Observable<U>): Operator<T, U>;
