import { Observable, Operator } from '../core/_api';
/**
 * 当前置 Observable 完成时，才发送之后产生的值
 * @param prerequisite
 */
export declare function delayWhen<T>(prerequisite: (value: T) => Observable<any>): Operator<T, T>;
