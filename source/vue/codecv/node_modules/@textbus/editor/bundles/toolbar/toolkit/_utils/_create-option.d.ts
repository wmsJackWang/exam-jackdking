import { Keymap } from '@textbus/core';
export interface OptionConfig {
    label?: string;
    iconClasses?: string[];
    classes?: string[];
    onClick(): void;
    keymap?: Keymap;
    disabled?: boolean;
}
export declare function createOption(config: OptionConfig): HTMLElement;
