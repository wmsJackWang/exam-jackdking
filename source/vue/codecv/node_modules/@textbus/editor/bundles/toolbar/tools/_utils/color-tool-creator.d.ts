import { QueryState, Formatter } from '@textbus/core';
import { Injector } from '@tanbo/di';
import { Observable } from '@tanbo/stream';
import { Palette } from './palette';
import { UISegmentDropdown } from '../../toolkit/_utils/_api';
export declare function colorToolCreator(injector: Injector, palette: Palette, formatter: Formatter<any>): {
    viewController: {
        elementRef: HTMLDivElement;
        onComplete: Observable<string | null>;
        onCancel: Observable<void>;
        reset(): void;
        update(newValue: any): void;
    };
    onInit(ui: UISegmentDropdown): void;
    useValue(value: any): void;
    queryState(): QueryState<any>;
};
