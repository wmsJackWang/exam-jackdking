import { FormRadioGroupParams, AttrState, FormItem } from './help';
export declare class FormRadio implements FormItem {
    private config;
    elementRef: HTMLDivElement;
    name: string;
    private readonly inputs;
    private readonly feedbackEle;
    constructor(config: FormRadioGroupParams);
    reset(): void;
    update(value?: any): void;
    getAttr(): AttrState<any>;
    validate(): boolean;
}
