import { FormSwitchParams, AttrState, FormItem } from './help';
export declare class FormSwitch implements FormItem<boolean> {
    private config;
    elementRef: HTMLDivElement;
    name: string;
    private input;
    private readonly feedbackEle;
    constructor(config: FormSwitchParams);
    reset(): void;
    update(value?: any): void;
    getAttr(): AttrState<boolean>;
    validate(): boolean;
}
