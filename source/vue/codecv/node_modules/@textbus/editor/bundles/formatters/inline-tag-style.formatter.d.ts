import { Formatter, FormatValue, VElement, VTextNode } from '@textbus/core';
import { Matcher, MatchRule } from './matcher';
import { FormatLoader } from '@textbus/platform-browser';
export declare class InlineTagStyleFormatLoader<T extends FormatValue> extends Matcher<T, Formatter<any>> implements FormatLoader<any> {
    styleName: string;
    forceMatchTags: boolean;
    constructor(styleName: string, formatter: Formatter<any>, rule: MatchRule, forceMatchTags?: boolean);
    match(element: HTMLElement): boolean;
    read(node: HTMLElement): {
        formatter: Formatter<any>;
        value: string | number;
    };
}
export declare class InlineTagStyleFormatter implements Formatter<any> {
    name: string;
    styleName: string;
    columned: boolean;
    priority: number;
    constructor(name: string, styleName: string);
    render(children: Array<VElement | VTextNode>, formatValue: string): VElement;
}
export declare class InlineTagLeafStyleFormatter extends InlineTagStyleFormatter {
    columned: boolean;
}
export declare const verticalAlignFormatter: InlineTagStyleFormatter;
export declare const textBackgroundColorFormatter: InlineTagLeafStyleFormatter;
export declare const verticalAlignFormatLoader: InlineTagStyleFormatLoader<FormatValue>;
export declare const textBackgroundColorFormatLoader: InlineTagStyleFormatLoader<FormatValue>;
